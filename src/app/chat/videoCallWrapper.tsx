import { FC } from "react";
type VideoCallWrapperComponent = {
  conversationId: string;
};

const VideoCallWrapper: FC<VideoCallWrapperComponent> = ({
  conversationId,
}) => {
  return <main></main>;
};

export default VideoCallWrapper;
